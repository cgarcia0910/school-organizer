import { Column, Entity, PrimaryGeneratedColumn } from 'typeorm';

@Entity('<%= entity %>')
export class <%= entityClassName %>Entity {
  @PrimaryGeneratedColumn()
  id!: number;

  @Column({ type: 'varchar', length: 255 })
  name!: string;
}