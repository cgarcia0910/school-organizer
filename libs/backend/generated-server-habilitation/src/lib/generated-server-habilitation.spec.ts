import { generatedServerHabilitation } from './generated-server-habilitation';

describe('generatedServerHabilitation', () => {
  it('should work', () => {
    expect(generatedServerHabilitation()).toEqual(
      'generated-server-habilitation',
    );
  });
});
