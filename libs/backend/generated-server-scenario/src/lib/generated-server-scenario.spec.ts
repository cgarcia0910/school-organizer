import { generatedServerScenario } from './generated-server-scenario';

describe('generatedServerScenario', () => {
  it('should work', () => {
    expect(generatedServerScenario()).toEqual('generated-server-scenario');
  });
});
