import { generatedServerSubject } from './generated-server-subject';

describe('generatedServerSubject', () => {
  it('should work', () => {
    expect(generatedServerSubject()).toEqual('generated-server-subject');
  });
});
