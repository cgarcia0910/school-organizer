import { FormContextDirective } from './form-context.directive';

describe('FormContextDirective', () => {
  it('should create an instance', () => {
    const directive = new FormContextDirective();
    expect(directive).toBeTruthy();
  });
});
