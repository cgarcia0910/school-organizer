import { AddScenarioDirective } from './add-scenario.directive';

describe('AddScenarioDirective', () => {
  it('should create an instance', () => {
    const directive = new AddScenarioDirective();
    expect(directive).toBeTruthy();
  });
});
