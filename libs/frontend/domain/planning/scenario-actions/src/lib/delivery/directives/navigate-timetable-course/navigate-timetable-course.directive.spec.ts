import { NavigateTimetableCourseDirective } from './navigate-timetable-course.directive';

describe('NavigateTimetableCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateTimetableCourseDirective();
    expect(directive).toBeTruthy();
  });
});
