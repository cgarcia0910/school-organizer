import { CalculateScenarioDirective } from './calculate-scenario.directive';

describe('CalculateScenarioDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateScenarioDirective();
    expect(directive).toBeTruthy();
  });
});
