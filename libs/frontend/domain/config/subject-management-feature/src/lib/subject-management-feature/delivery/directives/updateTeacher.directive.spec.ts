import { UpdateTeacherDirective } from './updateTeacher.directive';

describe('UpdateTeacherDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateTeacherDirective();
    expect(directive).toBeTruthy();
  });
});
