import { AddTeacherDirective } from './addTeacher.directive';

describe('AddTeacherDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTeacherDirective();
    expect(directive).toBeTruthy();
  });
});
