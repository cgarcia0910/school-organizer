import { DeleteTeacherDirective } from './deleteTeacher.directive';

describe('DeleteTeacherDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteTeacherDirective();
    expect(directive).toBeTruthy();
  });
});
