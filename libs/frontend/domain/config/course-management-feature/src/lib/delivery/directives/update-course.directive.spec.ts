import { UpdateCourseDirective } from './update-course.directive';

describe('UpdateCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateCourseDirective();
    expect(directive).toBeTruthy();
  });
});
