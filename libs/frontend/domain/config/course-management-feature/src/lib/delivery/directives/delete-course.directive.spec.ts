import { DeleteCourseDirective } from './delete-course.directive';

describe('DeleteCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteCourseDirective();
    expect(directive).toBeTruthy();
  });
});
